before running record_app

```mkdir results```


run record_app after setting parameters **board_width, board_height, camID** in ***config.cfg***

When board is detected, frame can be saved using key 's'

When enough frames have been saved run calib_app after having specified parameters **images_dir** & **square_size** in config.cfg